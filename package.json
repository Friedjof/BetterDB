{
  "name": "db_ticket",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "start:production": "USE_CHROMIUM_PATH=true next start",
    "lint": "next lint",
    "lint:fix": "next lint --fix",
    "type-check": "tsc --noEmit || echo 'No TypeScript config found'",
    "test": "echo 'No tests specified' && exit 0",
    "docker:build": "docker build -t betterbahn .",
    "docker:run": "docker run -p 3000:3000 betterbahn",
    "docker:dev": "docker build -t betterbahn:dev . && docker run -p 3000:3000 betterbahn:dev",
    "deploy": "docker build"
  },
  "dependencies": {
    "db-vendo-client": "^6.8.2",
    "hafas-client": "^6.3.5",
    "next": "15.3.4",
    "puppeteer": "^24.10.2",
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "devDependencies": {
    "@tailwindcss/postcss": "^4",
    "tailwindcss": "^4"
  }
}